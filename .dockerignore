# Node modules and build output
node_modules
.next
out

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Env files (optional — only if you don’t want to bake them into image)
.env
.env.*
!.env.production  # Optional: allow production env if needed

# OS and editor junk
.DS_Store
*.swp
*.swo
.idea
.vscode

# Docker junk (if reusing compose)
docker-compose.override.yml
